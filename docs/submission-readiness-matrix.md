# LP-0008 strict submission-readiness matrix

Status: **not ready for final Lambda Prize submission yet**. Latest strict evidence bundle completed with `ok_with_blockers`, and follow-up hardening is pushed at commit `9c2175132fcec5832b1dda67627e93f091914eec`: no shell failures, one known blocked step (`paid-a2a-live`) caused by the rc3 wallet transfer proof panic.

This matrix separates working implementation evidence from strict prize acceptance. It exists to prevent validator/CI success from being mistaken for complete acceptance readiness.

| Prize criterion | Current status | Evidence | Remaining work |
|---|---|---|---|
| Load agent inside Logos Core alongside storage/messaging/wallet without modifying those modules | Partial | `scripts/run_logoscore_integration.sh all` co-loads `agent_module`, `storage_module`, and `delivery_module`; wallet FFI path is available on Darwin. | Prove wallet as a co-loaded Logos Core module or keep Darwin FFI boundary explicit. |
| Agent has own shielded LEZ account and can send/receive tokens | Partial | rc3 wallet FFI funded send verified on public testnet, tx `5dcf1b318ff5aadf5a8bff9843de71184b0f1c16e6234163373315a144df1fd3`. | Add receive-token evidence and document platform limits; Linux stays fail-closed. |
| Single CLI deploy/configure on any machine using headless Logos Core | Closed for supported headless environment | `scripts/run_headless_deploy_evidence.py` proves headless Logos Core deployment: Nix build, storage/delivery/agent loads through `logoscore`, `meta.configure` owner/task topics, `meta.status`, and `agent.card`. | Platform prerequisites remain explicit; wallet FFI is Darwin-bound while Linux CI stays fail-closed. |
| Owner interacts in real time from separate Logos app/Basecamp via Logos Messaging, no intermediary server | Partial / video-gated | `scripts/run_basecamp_owner_approval_evidence.py` proves separate owner topic notification, retry/reject/approve state machine, and live Logos Messaging delivery IDs; `scaffold.toml`/`.lgx` prove Basecamp packaging readiness. | Final video still must show the reviewer-facing owner surface / Basecamp interaction; no intermediary server is used. |
| Spending threshold holds above-threshold for owner approval and executes below-threshold autonomously | Mostly closed | Spending gate permits below-limit sends; above-limit sends create persisted approval records, send owner-topic notifications, support retry/reject/approve, enforce timeout on approval/retry, and record approved execution in wallet history. | Still need Basecamp owner-channel UI proof for reviewer-facing approval. |
| All default skills implemented and documented | Strict matrix closed with honest boundaries | `scripts/run_strict_skill_matrix.py` verifies 27 registered skills: the 23 original spec skills plus approval/receive/complete lifecycle surface. It covers success or bounded behavior plus fail-closed/negative behavior where semantically applicable, including share semantics, group create/join, wallet approvals, program boundaries, and malformed A2A envelopes. | `program.call` and `program.deploy` intentionally fail closed until Logos Core exposes a stable in-process LEZ program API; live LEZ wallet transfers remain covered by separate wallet evidence/blocker docs. |
| A2A-compatible Agent Cards and task lifecycle over Logos Messaging | Strict transport closed with payment blocker | Agent Cards are published to the discovery registry; `scripts/run_lp0008_deep_verify.py` and `scripts/run_paid_a2a_live_evidence.py` prove `agent.task` emits a task envelope over live `delivery_module`/Logos Messaging and persists queued -> transport_sent -> working -> completed. | LEZ payment tx binding remains blocked by rc3 wallet transfer panic. |
| Two+ agents discover, execute task, and transfer LEZ payment autonomously | Partial | `scripts/run_lp0008_deep_verify.py` proves `agent.task` emits an A2A task envelope over live `delivery_module`/Logos Messaging (`transport.result.mode == live`) and completes the requested skill. `demo.sh` proves the per-task LEZ payment hook is recorded in task lifecycle with `payment_submitted`. | Upgrade from deterministic/simulated payment-hook evidence to a confirmed live LEZ payment tied to task acceptance between separate funded agents. |
| Three illustrative use cases demonstrated end-to-end on LEZ testnet | Partial | `scripts/run_three_use_cases_demo.sh` now proves personal file vault, agent services marketplace payment hook, and multi-agent workflow evidence with `summary.json`. Live Logos Messaging A2A transport is proven separately by deep verify; live LEZ payment remains separate wallet evidence. | Upgrade the marketplace payment hook to a confirmed live LEZ transfer between separate funded agents, and record reviewer-facing video. |
| Three separate agents deployed on LEZ testnet, one per default skill category | Closed for three funded persisted agent identities | `scripts/setup_three_live_agents.sh` and `scripts/verify_three_live_agents.py` prove Alpha Storage, Beta Messaging, and Gamma Chain have separate persisted state dirs, task topics, owner topics, Agent Cards, and funded public-testnet wallet account identifiers. | Final video should narrate the three identities and evidence summary. |
| Full documentation and clean public repo | Mostly closed | README, docs, CI, MIT license, evidence docs, headless deployment, sidecar skill-extension, and fresh-clone reproduction are in the public repo. | Final video URL and known upstream blockers remain explicit. |
| Skill interface allows adding skills without modifying core agent module | Closed for sidecar-extension model | `docs/skill-extension-guide.md` and `scripts/run_skill_extension_evidence.py` document and validate a sidecar skill-extension contract over A2A/Logos Messaging without core module edits. | Does not claim hot-loaded in-process C++ plugins. |
| Basecamp owner-facing interface and loadable assets | Partial | `scaffold.toml`; `.lgx` build path. | Real Basecamp install/launch and owner-channel interaction proof. |
| Recovery from transient failures/node restarts without losing pending task state | Mostly closed | `scripts/run_resilience_evidence.sh` creates a pending approval, reloads it from `approvals.json` across a new plugin process, forces timeout, verifies retry/approve fail closed, and persists `timed_out`. | Still does not prove distributed network-partition recovery or Basecamp GUI recovery. |
| Above-threshold transactions unreachable owner: retry then timeout/fail closed | Mostly closed | Above-threshold sends create pending approvals, send owner-topic notifications, retry notifications, reject/approve, and `scripts/run_resilience_evidence.sh` verifies expired approvals fail closed and do not execute. | Still needs Basecamp owner-channel UI proof for the owner-facing approval surface. |
| Skill failures isolated | Closed for module-level evidence | `scripts/run_resilience_evidence.sh` proves a failed `agent.task` does not poison storage upload/download. `scripts/run_strict_skill_matrix.py` adds six parallel live `messaging.send` calls with distinct message IDs and verifies agent/storage/delivery modules remain loaded after invalid-topic and fail-closed cases. | Does not claim distributed multi-node network-partition tolerance beyond loaded-module/process-boundary evidence. |
| CU cost documented | Partial | `docs/cu-costs.md` records the latest public-testnet wallet tx and states that current rc3 wallet output does not expose a stable CU field; program ops fail closed with no submitted tx. | Add measured CU numbers if/when Logos tooling exposes them, and add proof-mode evidence if required by reviewers. |
| LEZ devnet/testnet deployment/testing | Mostly closed with payment blocker | Public-testnet wallet/agent identities, live Logos Messaging A2A, three-agent profiles, headless Logos Core deployment, and fresh-clone reproduction are retained. | Paid A2A LEZ payment binding and program deploy/call remain upstream/tooling-blocked. |
| E2E tests against LEZ sequencer included in CI | Manual opt-in live CI plus strict local gate | `docs/live-strict-evidence.workflow.yml` provides a workflow template that can be copied to `.github/workflows/live-strict-evidence.yml` with a workflow-scoped token for macOS/Nix/live prerequisites; default CI remains build/demo-safe. | Do not enable live funded-wallet gates by default without secrets/tooling. |
| CI green on default branch | Met | GitHub Actions green on default branch. | Keep green after new work. |
| README documents CLI and Logos app owner-channel usage | Partial | CLI docs exist; Basecamp readiness docs exist. | Add true owner-channel guide after Basecamp proof. |
| Reproducible E2E demo script against local sequencer with `RISC0_DEV_MODE=0` | Upstream/tooling-blocked and narrowed | `scripts/run_program_cu_boundary_evidence.py` records `RISC0_DEV_MODE=<unset>` boundary, fail-closed program call/deploy, and `docs/cu-costs.md` documents missing stable CU/proof fields. | Add measured proof/CU numbers only if Logos tooling exposes them; current submission must not claim them. |
| Recorded video demo | Not yet met | Scripts/narration prepared. | Record final terminal + Basecamp evidence after strict gaps close. |

## Current go/no-go

- Mechanical repository hygiene: **go**.
- Strict Lambda Prize final submission: **no-go** until final video is recorded and Evi explicitly accepts the remaining upstream-blocked risks (`paid-a2a-live` LEZ tx binding and in-process program deploy/call/CU proof limits).

## Non-video hardening added after final audit

- Headless Logos Core deployment: `scripts/run_headless_deploy_evidence.py`.
- Sidecar skill-extension path: `docs/skill-extension-guide.md` + `scripts/run_skill_extension_evidence.py`.
- Fresh-clone reproduction: `scripts/run_fresh_clone_strict_repro.sh`.

